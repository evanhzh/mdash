import React from 'react'
import { Navbar } from 'react-bootstrap'

const NavigationBar = () => {
    return (

        <Navbar expand="lg" variant="light" bg="dark">
            <Navbar.Text className="titleStyle">M-Dash</Navbar.Text>
        </Navbar>

    )
}

export default NavigationBar
